# Dependency-light RAG contract and evidence environment.
# The deterministic embedder must not download torch or an embedding model.

numpy>=1.24,<3
pytest>=8,<9
psycopg2-binary>=2.9,<3
python-dotenv>=1,<2
